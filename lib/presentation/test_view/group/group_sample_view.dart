import 'package:flutter/material.dart';

class SampleGroupWidget extends StatelessWidget {
  const SampleGroupWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
