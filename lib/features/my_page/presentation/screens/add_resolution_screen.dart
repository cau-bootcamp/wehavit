import 'package:flutter/material.dart';

class AddResolutionScreen extends StatelessWidget {
  const AddResolutionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        shadowColor: Colors.transparent,
      ),
      body: Container(),
    );
  }
}
